# AI 工作原则

## 任务验证规则

1. **所有任务必须达到目标并验证通过才算完成**
   - 在声称"已完成"或"正常运行"之前，必须用实际命令/工具验证服务状态
   - 不能仅凭启动命令的返回结果判断服务正常运行
   - 必须实际访问端口/端点确认可连接
   - 例如：启动 Web 服务后，必须 curl/wget/Invoke-WebRequest 验证 http://localhost:PORT 可访问

## 工作态度

- 严谨验证，不主观臆断
- 发现问题立即解决，不拖延
- 验证未通过则任务未完成
